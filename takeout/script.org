* node
sudo npm install -g mocha
sudo npm install -g typescript
sudo npm install -g truffle
sudo npm install -g ganache


* daostack
git clone https://github.com/daostack/DAOstack-Hakers-Kit.git
cd starter-template
npm i
nano .env
    exemplo = data/example.env
    private key = ganache PK
nano data/testDaoSpec.json
 
?opcional ?  npm run launch:docker     
? ?          nano docker-compose.yml
??           npm run logs:ganache 
?            nano subgraph/.env      "exemple.env"
             npm run deploy-graph
 testa api >         localhost:8000
 testa frontend >    localhost:3000



npm run migrate
   verifica Account > rinkeby etherscan 1 tx + 3 ?




// dependencia para synapse
sudo apt install libjpeg-dev


* linux
** docker
sudo apt update
sudo apt install docker.io
sudo systemctl start docker
sudo systemctl enable docker


* proxy
apt
env
pip
node
git config --global http.proxy http://10.1.0.16:3128
curl -x http://proxy_server:proxy_port --proxy-user username:password -L http://url
chromium

** proxy emacs
(setq url-proxy-services
   '(("no_proxy" . "^\\(localhost\\|10\\..*\\|192\\.168\\..*\\)")
     ("http" . "proxy.com:8080")
     ("https" . "proxy.com:8080")))

(setq url-http-proxy-basic-auth-storage
    (list (list "proxy.com:8080"
                (cons "Input your LDAP UID !"
                      (base64-encode-string "LOGIN:PASSWORD")))))


** docker
sudo apt update
sudo apt install apt-transport-https ca-certificates curl software-properties-common
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu bionic stable"
sudo apt update
apt-cache policy docker-ce
sudo apt install docker-ce
sudo systemctl status docker


curl -x http://10.1.0.16:3128 --proxy-user joao.nitzsche:  -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
curl -x http://proxy_server:proxy_port --proxy-user username:password -L http://url


* Links
** instalar docker
https://www.digitalocean.com/community/tutorials/como-instalar-e-usar-o-docker-no-ubuntu-18-04-pt
